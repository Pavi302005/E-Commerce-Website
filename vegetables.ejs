<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>All Vegetables - Fresh Grocery</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
<link rel="stylesheet" href="/styles.css">
</head>
<style>
    body {
    margin: 0;
    font-family: Arial, sans-serif;
    background-color: #f7f7f7;
}

.section-title {
    text-align: center;
    margin: 50px 0 30px;
    font-size: 36px;
    color: #333;
}

.products-section {
    padding: 20px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 30px;
    max-width: 1200px;
    margin: auto;
}

.product-card {
    background-color: white;
    border-radius: 10px;
    overflow: hidden;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    transition: transform 0.3s, box-shadow 0.3s;
    position: relative;
}

.product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 25px rgba(0,0,0,0.2);
}

.product-card img {
    width: 100%;
    height: 200px;
    object-fit: cover;
}

.product-info {
    padding: 15px;
}

.product-info h3 {
    margin: 0 0 10px;
    font-size: 20px;
    color: #333;
}

.product-info p {
    margin: 5px 0;
    color: #666;
}

.price {
    font-weight: bold;
    color: #ff5722;
}

.rating {
    color: #ffc107;
}

.like-icon {
    position: absolute;
    top: 15px;
    right: 15px;
    color: #ff4d4d;
    font-size: 22px;
    cursor: pointer;
    transition: transform 0.3s;
}

.like-icon:hover {
    transform: scale(1.3);
}

</style>
<body>

<h2 class="section-title">All Vegetables</h2>
<div class="products-section">
    <% vegetables.forEach(veg => { %>
        <div class="product-card">
            <i class="fa-solid fa-heart like-icon"></i>
            <img src="<%= veg.img %>" alt="<%= veg.name %>">
            <div class="product-info">
                <h3><%= veg.name %></h3>
                <p class="price"><%= veg.price %></p>
                <p class="rating">
                    <% for(let i=0;i<veg.rating;i++){ %>⭐<% } %>
                    <% for(let i=veg.rating;i<5;i++){ %>☆<% } %>
                </p>
            </div>
        </div>
    <% }) %>
</div>

</body>
</html>
